#include "core/PoolAllocator.h"

// PoolAllocator is header-only (template), but this .cpp exists so that:
//   1. It appears in SOURCES for IDE indexing / link-time discovery.
//   2. Explicit template instantiations can live here if needed.

namespace glory {
// intentionally empty
} // namespace glory
