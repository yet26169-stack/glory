#include "core/FrameAllocator.h"

// FrameAllocator is header-only (inline), but this .cpp exists so that:
//   1. It appears in SOURCES for IDE indexing / link-time discovery.
//   2. Future non-inline helpers (e.g. statistics logging) have a home.

namespace glory {
// intentionally empty
} // namespace glory
