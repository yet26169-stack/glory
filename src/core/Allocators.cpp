#include "core/FrameAllocator.h"
#include "core/PoolAllocator.h"

namespace glory {

// FrameAllocator and PoolAllocator are header-only (inline/template).
// This file provides a translation unit for IDE indexing and future
// non-inline helpers or explicit template instantiations.

} // namespace glory
